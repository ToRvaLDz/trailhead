---
description: "Adopt an existing project: map the codebase once, then go lean"
argument-hint: ""
---

Invoke the `trailhead` skill for the **adopt** action: call the Skill tool with skill name `trailhead-monolith` and arguments `adopt $ARGUMENTS`, then carry out the skill's instructions for that verb. Do not re-implement the behaviour here. The skill is the single source of truth.
