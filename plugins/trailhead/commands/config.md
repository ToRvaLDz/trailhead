---
description: Guided menu setup, or get/set trailhead config
argument-hint: "[get|set …]"
---

Invoke the `trailhead` skill for the **config** action: call the Skill tool with skill name `trailhead-monolith` and arguments `config $ARGUMENTS`, then **read the skill's `references/configuration.md` and follow it**: that file holds the keys, their semantics, and the guided setup. Do not re-implement the behaviour here. The skill is the single source of truth.
