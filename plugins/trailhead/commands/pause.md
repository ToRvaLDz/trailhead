---
description: Checkpoint the ticket in play so it can be resumed later
argument-hint: "[note]"
---

Invoke the `trailhead` skill for the **pause** action: call the Skill tool with skill name `trailhead` and arguments `pause $ARGUMENTS`, then **read the skill's `references/teamwork.md` and follow it** — that file holds the pausing & resuming protocol. Do not re-implement the behaviour here — the skill is the single source of truth.
