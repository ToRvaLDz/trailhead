---
description: Work the next frontier ticket, or the one you name
argument-hint: "[ticket]"
---

Invoke the `trailhead` skill for the **work** action: call the Skill tool with skill name `trailhead` and arguments `work $ARGUMENTS`, then carry out the skill's instructions for that verb (under the repo's `trailhead:conventions` `isolation: worktree` or `clone`, work the ticket in its own isolated workspace on a `trailhead/t<n>` branch, set up at work-start, per `references/teamwork.md`). Do not re-implement the behaviour here. The skill is the single source of truth.
