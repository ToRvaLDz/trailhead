---
description: Resume a paused ticket from its checkpoint
argument-hint: "[ticket]"
---

Invoke the `trailhead` skill for the **resume** action: call the Skill tool with skill name `trailhead` and arguments `resume $ARGUMENTS`, then **read the skill's `references/teamwork.md` and follow it**: that file holds the pausing & resuming protocol. Do not re-implement the behaviour here. The skill is the single source of truth.
