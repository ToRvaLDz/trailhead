---
// Replaces Starlight's default <ThemeSelect>. The /docs/ section is
// dark-only (matching the landing page), so this intentionally renders
// nothing: no light/dark/auto picker, no <starlight-theme-select> element.
---
